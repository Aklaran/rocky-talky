# =============================================================================
# Basecamp Environment Variables
# Copy to .env and fill in values: cp .env.example .env
# =============================================================================

# --- App ---
NODE_ENV=development
PORT=3000

# --- Database ---
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Dev default matches docker-compose.yml postgres service
DATABASE_URL=postgresql://basecamp:basecamp_dev@localhost:5432/basecamp

# --- Auth ---
# Must be at least 32 characters. Generate with: openssl rand -hex 32
SESSION_SECRET=change-me-to-a-real-secret-at-least-32-chars-long
# Set to false for Tailscale deployments (no HTTPS). Defaults to true in production.
# COOKIE_SECURE=false

# --- AI ---
# Provider: openai, anthropic, or mock. Omit to disable AI (graceful fallback).
# AI_PROVIDER=openai
# AI_MODEL=gpt-4o-mini
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# Optional: override the system prompt for all conversations.
# AI_SYSTEM_PROMPT=You are a helpful assistant.

# --- Logging ---
# LOG_LEVEL=debug  # fatal, error, warn, info, debug, trace
