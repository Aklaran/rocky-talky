# =============================================================================
# Basecamp Environment Variables
# Copy to .env and fill in values: cp .env.example .env
# =============================================================================

# --- App ---
NODE_ENV=development
PORT=3000

# --- Database ---
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Dev default matches docker-compose.yml postgres service
DATABASE_URL=postgresql://basecamp:basecamp_dev@localhost:5432/basecamp

# --- Auth (Phase 2+) ---
# Must be at least 32 characters. Generate with: openssl rand -hex 32
SESSION_SECRET=change-me-to-a-real-secret-at-least-32-chars-long

# --- AI (Phase 4+) ---
# AI_PROVIDER=openai
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# OLLAMA_BASE_URL=http://localhost:11434

# --- Logging ---
# LOG_LEVEL=debug  # fatal, error, warn, info, debug, trace
